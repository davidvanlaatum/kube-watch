{
  "name": "kube-watch-web",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cd src && vite",
    "build-frontend": "cd src && vite build --outDir ../dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  }
}
